@model IEnumerable<RecipeHub.Mvc.Models.RecipeIngredient>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Recipe
            </th>
            <th>
                Ingredient
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Unit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                <td>
                    @item.Recipe?.Title
                </td>
                <td>
                    @item.Ingredient?.Name
                </td>
            <td>
                @Html.DisplayFor(model => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(model => item.Unit)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecipeIngredientId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecipeIngredientId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecipeIngredientId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
